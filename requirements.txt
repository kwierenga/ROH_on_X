# Core dependencies for xroh_sim.py
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
scikit-learn>=1.3

# Optional ROH callers for real-data work (NOT required by xroh_sim.py):
#   scikit-allel  — note: roh_mhmm allocates per-base-pair arrays and OOMs at
#                   chromosome scale; roh_poissonhmm needs `protopunica` and was
#                   degenerate on synthetic data. Prefer PLINK --homozyg or
#                   `bcftools roh` for real datasets.
# scikit-allel
# hmmlearn
# protopunica
